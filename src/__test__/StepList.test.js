import React from 'react';
import {shallow} from 'enzyme';
import StepList from '../components/StepList';

describe('StepList tests',()=>{
    it('should render StepList',()=>{
        const wrapper = shallow(<StepList />)
    })
})